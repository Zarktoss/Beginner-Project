#days_in_feb = 28

first_num = input ('Enter first number ')
second_num = input ('Enter second number ') # If we put an input we allways get strings so we have to convert after the numbers.
print(int(first_num) + int(second_num)) # We put str/int to transform de number to concatenate it with the string or sum numbers